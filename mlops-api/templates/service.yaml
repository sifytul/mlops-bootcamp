apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlops-api.fullname" . }}
  labels: {{- include "mlops-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector: {{- include "mlops-api.selectorLabels" . | nindent 4 }}
